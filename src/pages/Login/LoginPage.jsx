import React from 'react';
import LoginContainer from '../../containers/LoginContainer';
import { StyledLayout } from './login.styled';

const LoginPage = () => {
  return (
    <StyledLayout>
      <LoginContainer />
    </StyledLayout>
  );
};

export default LoginPage;
